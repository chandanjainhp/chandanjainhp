import { Router } from 'express';

const router = Router();

router.post('/pageview', (_req, res) => {
  res.json({});
});

router.post('/interaction', (_req, res) => {
  res.json({});
});

router.get('/dashboard', (_req, res) => {
  res.json({});
});

router.post('/contact', (_req, res) => {
  res.json({});
});

export default router;
